/** @type {import('next').NextConfig} */
const nextConfig = {
  // The appDir is enabled by default in App Router
  // No need to specify it in experimental anymore
  // other config options...
}

module.exports = nextConfig